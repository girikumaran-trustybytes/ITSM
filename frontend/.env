# Frontend environment variables
# Used during development and docker build

# API base URL for axios
# In development: leave empty (will use relative paths and vite proxy)
# In docker: use /api and nginx will proxy to backend container
VITE_API_BASE=/api
