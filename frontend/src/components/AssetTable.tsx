import React from 'react'
import type { Asset } from '../App'

export default function AssetTable({ assets }: { assets: Asset[] }) {
  return (
    <div className="asset-table">
      {/* Asset table is currently empty */}
    </div>
  )
}
