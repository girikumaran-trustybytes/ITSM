# Frontend Domain Modules

This folder groups domain-specific UI and API adapters:

- `tickets`
- `assets`
- `users`
- `suppliers`

Current implementation uses re-export wrappers to preserve existing behavior while allowing imports through module boundaries.
